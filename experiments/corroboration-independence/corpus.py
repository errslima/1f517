"""Experiment corpus: 60 findings-shaped claims about public artifacts.
30 true (all verified against primary sources during seed-corpus work,
2026-08-22), 30 planted false-but-plausible (mutations of verified facts,
each unambiguously wrong).

Ground truth is the load-bearing part: if a label is wrong the whole
experiment is garbage, so every TRUE claim traces to a source fetched this
session and every FALSE claim inverts or shifts a specific verified detail.
"""
import json

T = True
F = False

C = [
# ---------------- TRUE (30) ----------------
("pkg:pypi/pandas", "DataFrame.append and Series.append were removed in pandas 2.0; concat is the documented replacement.",
 "Calling DataFrame.append on a 2.x release raises AttributeError, and the 2.0.0 release notes list the removal with concat named instead.", T, "verified: pandas 2.0.0 whatsnew, GH 35407"),
("pkg:pypi/numpy", "The alias np.float raises an error from numpy 1.24, where its earlier deprecation expired.",
 "Evaluating np.float on 1.24 or later raises rather than returning a type.", T, "verified: numpy 1.24.0 release notes, gh-22607"),
("pkg:npm/node-fetch", "From version 3 the package is ESM-only and cannot be loaded with require(); version 2 remains CommonJS-compatible.",
 "require('node-fetch') on a v3 install fails, while the same call on v2 succeeds.", T, "verified: node-fetch README"),
("pkg:npm/chalk", "Chalk 5 is published as pure ESM and cannot be loaded with require(); CommonJS consumers are directed to version 4.",
 "require('chalk') fails on a v5 install and succeeds on v4.", T, "verified: chalk readme / v5.0.0 release notes"),
("pkg:npm/express", "Express 5 declares a minimum Node.js engine of 18 in its package manifest.",
 "The published package.json for express 5.x carries an engines.node value of >= 18.", T, "verified: express package.json, v5.2.1"),
("pkg:pypi/sqlalchemy", "Passing a bare SQL string to Connection.execute raises in SQLAlchemy 2.0; textual SQL requires text() or exec_driver_sql.",
 "conn.execute('SELECT 1') raises on a 2.x release rather than returning rows.", T, "verified: SQLAlchemy 2.0 migration guide"),
("pkg:pypi/urllib3", "urllib3 2.x supports only OpenSSL 1.1.1 or newer and raises ImportError against an ssl module built on older OpenSSL.",
 "Importing urllib3 2.x where ssl reports OpenSSL 1.0.2 raises ImportError naming the version requirement.", T, "verified: urllib3 v2 migration guide"),
("tool:ffmpeg", "With no explicit map option, automatic stream selection picks the highest-resolution video stream and the audio stream with the most channels.",
 "Given several video streams of differing resolution and no map option, the highest-resolution one is selected.", T, "verified: ffmpeg docs, stream selection"),
("tool:curl", "HTTP 3xx redirects are not followed unless the location option is supplied.",
 "A request to a URL returning 302 emits the redirect response rather than the final target's body, absent -L.", T, "verified: curl manpage"),
("tool:sqlite", "The default journaling mode is DELETE; write-ahead logging must be enabled explicitly via pragma.",
 "PRAGMA journal_mode on a newly created database returns delete rather than wal.", T, "verified: sqlite pragma docs"),
("spec:ietf/rfc9110", "RFC 9110 obsoletes RFC 7231.",
 "The RFC Editor entry for 9110 lists 7231 among the RFCs it obsoletes.", T, "verified: rfc-editor.org/info/rfc9110"),
("spec:ietf/rfc8259", "RFC 8259 requires JSON exchanged between systems outside a closed ecosystem to be encoded in UTF-8.",
 "The specification text states that such JSON text MUST be encoded using UTF-8.", T, "verified: rfc-editor.org/info/rfc8259"),
("pkg:pypi/pydantic", "BaseModel.dict is deprecated in pydantic v2 in favour of model_dump.",
 "Calling .dict() on a v2 model emits a deprecation warning and the migration guide maps it to model_dump.", T, "verified: pydantic v2 migration guide"),
("pkg:pypi/black", "Black's default target line length is 88 characters.",
 "Formatting with no line-length flag wraps at 88 columns.", T, "verified: Black current style docs"),
("tool:jq", "jq pretty-prints its JSON output by default; compact single-line output requires the compact-output flag.",
 "Piping an object through jq with no flags yields indented multi-line output.", T, "verified: jq manual"),
("tool:rsync", "A trailing slash on the source path copies the directory's contents without recreating the directory itself at the destination.",
 "rsync src/ dst/ places src's children directly in dst, while rsync src dst creates dst/src.", T, "verified: rsync manpage"),
("pkg:npm/react", "From React 17 the new JSX transform allows JSX without importing React into scope.",
 "A component file using JSX but no React import compiles under the new transform.", T, "verified: React new JSX transform post"),
("pkg:npm/typescript", "When compilerOptions omits strict entirely, the compiler behaves as if strict were true.",
 "An untyped function parameter raises TS7006 under a tsconfig that never mentions strict.", T, "verified empirically with tsc this session"),
("pkg:npm/node", "The global fetch stopped being marked experimental in Node.js 21.",
 "The Node.js globals documentation history records fetch as no longer experimental at v21.0.0.", T, "verified: nodejs.org globals docs"),
("pkg:pypi/pip", "PEP 668 specifies that installing into an externally managed interpreter outside a virtual environment fails unless an override flag is passed.",
 "pip install outside a venv on an interpreter carrying the marker exits with an error naming the override.", T, "verified: PEP 668"),
("pkg:pypi/uv", "uv's pip interface installs into a virtual environment by default and needs an explicit flag or python path to touch the system interpreter.",
 "uv pip install with no active venv and no --system does not modify the system interpreter.", T, "verified: uv pip compatibility docs"),
("tool:docker", "A shell-form CMD runs the process under a shell rather than as PID 1, so it does not receive Unix signals.",
 "A container built with shell-form CMD does not deliver SIGTERM to the named executable on stop.", T, "verified: Dockerfile reference"),
("spec:ietf/rfc9111", "RFC 9111 obsoletes RFC 7234.",
 "The RFC Editor entry for 9111 states that it obsoletes 7234.", T, "verified: rfc-editor.org/info/rfc9111"),
("spec:semver/2.0.0", "Semantic Versioning 2.0.0 requires the MAJOR version to increment for any backward incompatible change to the public API.",
 "The specification text states MAJOR MUST be incremented when backward incompatible changes are introduced.", T, "verified: semver.org"),
("spec:ietf/rfc7519", "RFC 7519 (JSON Web Token) is updated by RFC 8725.",
 "The RFC Editor entry for 7519 lists 8725 among the RFCs updating it.", T, "verified: rfc-editor.org/info/rfc7519"),
("spec:ietf/rfc6455", "RFC 6455 (The WebSocket Protocol) is updated by RFC 8441.",
 "The RFC Editor entry for 6455 lists 8441 among the RFCs updating it.", T, "verified: rfc-editor.org/info/rfc6455"),
("pkg:pypi/fastapi", "FastAPI serves interactive documentation at /docs and /redoc unless those URLs are set to None.",
 "A plain FastAPI() application responds 200 at /docs with no extra configuration.", T, "verified: FastAPI metadata docs"),
("tool:git", "With init.defaultBranch unset in every configuration layer, git's documented fallback initial branch name is master.",
 "The git-init manual describes the fallback name as currently master, noting it changes at Git 3.0.", T, "verified: git-scm.com/docs/git-init"),
("pkg:pypi/requests", "requests applies no default timeout; a call omitting the timeout argument can block indefinitely.",
 "The documentation states no timeout is applied by default and advises passing one explicitly.", T, "verified: requests advanced docs"),
("api:api.github.com", "The GitHub REST API primary rate limit for unauthenticated requests is 60 per hour.",
 "The rate limit documentation states 60 requests per hour for unauthenticated traffic.", T, "verified: GitHub REST rate-limit docs"),

# ---------------- PLANTED FALSE (30) ----------------
("pkg:pypi/pandas", "DataFrame.merge was removed in pandas 2.0; join is the documented replacement.",
 "Calling DataFrame.merge on a 2.x release raises AttributeError.", F, "FALSE: append was removed, merge still exists"),
("pkg:pypi/numpy", "The function np.array was removed in numpy 1.24 in favour of np.asarray.",
 "Evaluating np.array on 1.24 or later raises AttributeError.", F, "FALSE: np.array is core and present"),
("pkg:npm/node-fetch", "node-fetch v2 is ESM-only and cannot be loaded with require(), while v3 restored CommonJS support.",
 "require('node-fetch') fails on a v2 install and succeeds on v3.", F, "FALSE: inverted; v3 is the ESM-only one"),
("pkg:npm/chalk", "Chalk 4 is pure ESM and cannot be loaded with require(); CommonJS consumers must use Chalk 5.",
 "require('chalk') fails on a v4 install and succeeds on v5.", F, "FALSE: inverted; v5 is ESM"),
("pkg:npm/express", "Express 5 declares a minimum Node.js engine of 20 in its package manifest.",
 "The published package.json for express 5.x carries an engines.node value of >= 20.", F, "FALSE: it is >= 18"),
("pkg:pypi/sqlalchemy", "SQLAlchemy 2.0 requires every Connection.execute call to be wrapped in an explicit session.begin() block or it raises InvalidRequestError.",
 "conn.execute(text('SELECT 1')) outside session.begin() raises InvalidRequestError on 2.x.", F, "FALSE: fabricated requirement"),
("pkg:pypi/urllib3", "urllib3 2.x requires OpenSSL 3.0 or newer and raises ImportError against OpenSSL 1.1.1.",
 "Importing urllib3 2.x where ssl reports OpenSSL 1.1.1 raises ImportError.", F, "FALSE: 1.1.1 is the supported floor"),
("tool:ffmpeg", "With no explicit map option, automatic stream selection picks the lowest-resolution video stream to minimise output size.",
 "Given several video streams of differing resolution and no map option, the lowest-resolution one is selected.", F, "FALSE: inverted; highest resolution wins"),
("tool:curl", "HTTP 3xx redirects are followed by default; the location option exists to disable that behaviour.",
 "A request to a URL returning 302 fetches the final target without any flag.", F, "FALSE: inverted; -L enables following"),
("tool:sqlite", "The default journaling mode is WAL; rollback journalling must be selected explicitly via pragma.",
 "PRAGMA journal_mode on a newly created database returns wal.", F, "FALSE: inverted; DELETE is default"),
("spec:ietf/rfc9110", "RFC 9110 obsoletes RFC 2616.",
 "The RFC Editor entry for 9110 lists 2616 among the RFCs it obsoletes.", F, "FALSE: 2616 was obsoleted by the 723x series"),
("spec:ietf/rfc8259", "RFC 8259 permits JSON exchanged between independent systems to be encoded in UTF-8, UTF-16 or UTF-32 at the sender's discretion.",
 "The specification text lists all three encodings as acceptable for open interchange.", F, "FALSE: UTF-8 is required for open interchange"),
("pkg:pypi/pydantic", "BaseModel.dict was renamed to BaseModel.to_dict in pydantic v2.",
 "Calling .to_dict() on a v2 model returns the field mapping.", F, "FALSE: it is model_dump"),
("pkg:pypi/black", "Black's default target line length is 79 characters, matching PEP 8.",
 "Formatting with no line-length flag wraps at 79 columns.", F, "FALSE: default is 88"),
("tool:jq", "jq emits compact single-line JSON by default; the pretty-print flag is needed for indented output.",
 "Piping an object through jq with no flags yields one line.", F, "FALSE: inverted; pretty is default"),
("tool:rsync", "A trailing slash on the source path causes the source directory itself to be recreated at the destination.",
 "rsync src/ dst/ produces dst/src rather than placing src's children in dst.", F, "FALSE: inverted"),
("pkg:npm/react", "React 18 reinstated the requirement to import React into scope in any file using JSX.",
 "A component file using JSX without importing React fails to compile under React 18.", F, "FALSE: new transform still applies"),
("pkg:npm/typescript", "When compilerOptions omits strict entirely, the compiler behaves as if strict were false.",
 "An untyped function parameter compiles without TS7006 under a tsconfig that never mentions strict.", F, "FALSE: verified empirically, behaves as true"),
("pkg:npm/node", "The global fetch stopped being marked experimental in Node.js 18.",
 "The Node.js globals documentation history records fetch as no longer experimental at v18.0.0.", F, "FALSE: v18 removed the flag, v21 dropped experimental"),
("pkg:pypi/pip", "The PEP 668 marker file that blocks installs outside a virtual environment is named MANAGED-EXTERNALLY.",
 "A file named MANAGED-EXTERNALLY in the stdlib directory triggers the refusal.", F, "FALSE: it is EXTERNALLY-MANAGED"),
("pkg:pypi/uv", "uv's pip interface installs into the system interpreter by default when no virtual environment is active, matching pip's behaviour.",
 "uv pip install with no active venv modifies the system interpreter without any extra flag.", F, "FALSE: inverted; uv requires --system"),
("tool:docker", "An exec-form CMD runs the process under a shell wrapper, so it does not receive Unix signals; shell form is required for correct signal delivery.",
 "A container built with exec-form CMD does not deliver SIGTERM to the named executable on stop.", F, "FALSE: inverted"),
("spec:ietf/rfc9111", "RFC 9111 obsoletes RFC 7231.",
 "The RFC Editor entry for 9111 states that it obsoletes 7231.", F, "FALSE: 9111 obsoletes 7234; 9110 obsoletes 7231"),
("spec:semver/2.0.0", "Semantic Versioning 2.0.0 requires the MINOR version to increment for a backward incompatible change to the public API, reserving MAJOR for complete rewrites.",
 "The specification text ties MINOR increments to backward incompatible changes.", F, "FALSE: MAJOR handles incompatible changes"),
("spec:ietf/rfc7519", "RFC 7519 (JSON Web Token) holds Internet Standard status.",
 "The RFC Editor entry for 7519 shows its status as Internet Standard.", F, "FALSE: it is Proposed Standard"),
("spec:ietf/rfc6455", "RFC 6455 (The WebSocket Protocol) holds Internet Standard status.",
 "The RFC Editor entry for 6455 shows its status as Internet Standard.", F, "FALSE: it is Proposed Standard"),
("pkg:pypi/fastapi", "FastAPI serves no documentation endpoints unless docs_url is set explicitly; a default application exposes neither /docs nor /redoc.",
 "A plain FastAPI() application returns 404 at /docs.", F, "FALSE: inverted; both are on by default"),
("tool:git", "Since Git 2.28 the default initial branch name for a new repository is main.",
 "git init with no configuration on 2.28 or later creates a branch named main.", F, "FALSE: 2.28 added init.defaultBranch; default stayed master"),
("pkg:pypi/requests", "requests applies a default timeout of 30 seconds when the timeout argument is omitted.",
 "A call omitting timeout against an unresponsive server raises after roughly 30 seconds.", F, "FALSE: no default timeout exists"),
("api:api.github.com", "The GitHub REST API primary rate limit for unauthenticated requests is 5000 per hour.",
 "The rate limit documentation states 5000 requests per hour for unauthenticated traffic.", F, "FALSE: 5000 is the authenticated limit; 60 unauthenticated"),
]

claims = [{"id": f"c{i+1:03d}", "subject": s, "claim": c, "expectation": e,
           "truth": t, "note": n} for i, (s, c, e, t, n) in enumerate(C)]

if __name__ == "__main__":
    print(json.dumps(claims, indent=1))
